# Core Flask
flask==3.0.0
flask-cors==4.0.0
gunicorn>=21.2.0

# Data processing (use pre-built wheels)
# Note: numpy 1.26.2 doesn't have wheels for Python 3.13+
# Using 1.26.4+ which has better Python 3.13 support
numpy>=1.26.4,<2.0.0
pandas>=2.2.0,<3.0.0
matplotlib>=3.8.2,<4.0.0

# Google Cloud
google-cloud-bigquery==3.13.0
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0

# AI/ML
openai>=1.0.0

# Database
supabase>=2.0.0

# Authentication
clerk-backend-api>=4.2.0

# Utilities
python-dotenv>=1.0.0

celery[redis]>=5.3.6
redis>=5.0.0

# PDF parsing for layout learning (try multiple libraries for better extraction)
PyPDF2>=3.0.0
pdfplumber>=0.10.0  # Better for complex layouts
pymupdf>=1.23.0  # Better for image-based PDFs (PyMuPDF)

clerk-backend-api>=1.0.0
